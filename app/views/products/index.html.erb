<div class="container-fluid">

  <div class="row">
    <h1><%= @category.name %></h1>
    
    <%= link_to "New #{@category.name.singularize}", new_category_product_path(@category.id) %>
  </div>

  <%= render partial: 'product_details', collection: @products, as: :product %>

</div>